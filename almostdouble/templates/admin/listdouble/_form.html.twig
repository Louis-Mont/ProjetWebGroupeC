{{ form_start(form) }}
<div class="row">
	<div class="col-md-2">{{ form_row(form.couples) }}</div>
</div>
{{ form_widget(form) }}
<button class="btn btn-primary">{{ button|default('Enregister') }}</button>
{{ form_end(form) }}
